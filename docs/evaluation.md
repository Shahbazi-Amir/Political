# Evaluation

سه سطح جدا: Unit/Security/Adversarial، Synthetic plumbing fixtures، و Real curated political benchmark.
Synthetic case هرگز production accuracy را بالا نمی‌برد. پرونده واقعی فقط پس از ثبت ground-truth sources، reference date، human review و review timestamp به `verified` تبدیل می‌شود.
Metric کلیدی `false_high_confidence_rate` است. تا dataset معنی‌دار وجود نداشته باشد گزارش باید بگوید: `Production political accuracy is not yet statistically established.`
